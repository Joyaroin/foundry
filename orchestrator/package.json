{
  "name": "@joyaroin/foundry",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "The unattended half of the foundry pipeline: spec -> tickets -> parallel build -> PR -> merge.",
  "bin": {
    "foundry": "./src/cli.ts"
  },
  "scripts": {
    "foundry": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.250"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
